# TUPLE METHODS

a = (1, 7, 2, 4, 5, 1, 2, 1)
print(a.count(1))   # a count (1) will return number of times 1 occurs in a TUPLE. 
print(a.index(1))   # will return the index of first occurrence of 1 in a TUPLE.
print(2 in a)       # check if the item is exists in tuple or not.